{
  "name": "leaner-monorepo",
  "private": true,
  "description": "Leaner.js repository",
  "type": "module",
  "workspaces": [
    "packages/leaner",
    "examples/benchmark",
    "examples/todomvc"
  ],
  "scripts": {
    "build": "npm run -w packages/leaner -w examples/benchmark -w examples/todomvc build",
    "test": "npm run -w packages/leaner test"
  }
}
