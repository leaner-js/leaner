{
  "name": "leaner-monorepo",
  "private": true,
  "description": "Leaner.js repository",
  "type": "module",
  "workspaces": [
    "packages/leaner",
    "examples/todomvc"
  ],
  "scripts": {
    "build": "npm run -w packages/leaner -w examples/todomvc build",
    "test": "npm run -w packages/leaner test"
  }
}
